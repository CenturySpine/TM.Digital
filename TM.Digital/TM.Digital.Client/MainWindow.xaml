<Window x:Class="TM.Digital.Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TM.Digital.Client"
        xmlns:board="clr-namespace:TM.Digital.Model.Board;assembly=TM.Digital.Model"
        xmlns:resources="clr-namespace:TM.Digital.Model.Resources;assembly=TM.Digital.Model"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        d:DataContext="{d:DesignInstance {x:Type local:MainWindowViewModel}}"
        Title="MainWindow"  d:DesignHeight="800" d:DesignWidth="1000">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="2">
            <UniformGrid Columns="3">
                
                <TextBlock Text="Players" Margin="2"></TextBlock>
                <TextBox Text="{Binding NumberOfPlayers, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="2"></TextBox>
            </UniformGrid>
            <UniformGrid Columns="2">
                <TextBlock Text="Server's address"  Margin="2"/>
                <TextBox Margin="2" Text="{Binding Server}"></TextBox>
            </UniformGrid>
            <Button Command="{Binding StartGameCommand}" Content="Start Game" Margin="2" Padding="2"></Button>

        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="2">
            <UniformGrid Columns="2">
                <TextBlock Text="Player's name" Margin="2"/>
                <TextBox Margin="2" Text="{Binding PlayerName}"></TextBox>
            </UniformGrid>
            <Button Command="{Binding AddPlayerCommand}" Content="Join" Margin="2" Padding="2"></Button>
        </StackPanel>
        <ItemsControl Grid.Row="2" ItemsSource="{Binding Board.BoardLines}" HorizontalAlignment="Center" VerticalAlignment="Center">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid Background="Transparent" >
                        <ItemsControl ItemsSource="{Binding BoardPlaces}" Margin="0,-5" HorizontalAlignment="Center">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button>
                                        <Button.Template>
                                            <ControlTemplate TargetType="{x:Type Button}">
                                                <Grid Background="Transparent" >

                                                    <Path x:Name="placeOutline" Width="70" Height="80"
                                                    Data="M35,0 70,20 70,60 35,80 0,60 0,20 35,0"
                                                    Fill="BurlyWood"
                                                    Margin="3,0"
                                                    StrokeThickness="2">
                                                        <Path.Style>
                                                            <Style TargetType="{x:Type Path}">
                                                                <Setter Property="Stroke" Value="Orange"></Setter>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding Reserved.ReservedFor}" Value="{x:Static board:ReservedFor.Ocean}">
                                                                        <Setter Property="Stroke" Value="Blue"></Setter>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Path.Style>
                                                    </Path>
                                                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                                        <TextBlock TextAlignment="Center"  Text="{Binding Name}" VerticalAlignment="Center" 
                                                           HorizontalAlignment="Center" 
                                                           FontSize="10" TextWrapping="Wrap" Margin="2">
                                                            <TextBlock.Style>
                                                                <Style TargetType="{x:Type TextBlock}">
                                                                    <Setter Property="Visibility" Value="Visible"></Setter>
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding Name}" Value="{x:Null}">
                                                                            <Setter Property="Visibility" Value="Collapsed"></Setter>
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </TextBlock.Style>
                                                        </TextBlock>
                                                        <ItemsControl ItemsSource="{Binding PlacementBonus}" HorizontalAlignment="Center">
                                                            <ItemsControl.Style>
                                                                <Style TargetType="{x:Type ItemsControl}">
                                                                    <Setter Property="Visibility" Value="Visible" />
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding PlacementBonus.Count}" Value="0">
                                                                            <Setter Property="Visibility" Value="Collapsed" />
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </ItemsControl.Style>
                                                            <ItemsControl.ItemsPanel>
                                                                <ItemsPanelTemplate>
                                                                    <StackPanel Orientation="Horizontal"></StackPanel>
                                                                </ItemsPanelTemplate>
                                                            </ItemsControl.ItemsPanel>
                                                            <ItemsControl.ItemTemplate>
                                                                <DataTemplate>
                                                                    <Path Width="15" Height="15" Stretch="Uniform" Margin="1">
                                                                        <Path.Style>
                                                                            <Style TargetType="{x:Type Path}">
                                                                                <Style.Triggers>
                                                                                    <DataTrigger Binding="{Binding BonusType}" Value="{x:Static resources:ResourceType.Card}">
                                                                                        <Setter Property="Data" Value="{Binding Source={StaticResource card}, Path=Data}" />
                                                                                        <Setter Property="Fill" Value="Black" />
                                                                                    </DataTrigger>
                                                                                    <DataTrigger Binding="{Binding BonusType}" Value="{x:Static resources:ResourceType.Titanium}">
                                                                                        <Setter Property="Data" Value="{Binding Source={StaticResource starbox}, Path=Data}" />
                                                                                        <Setter Property="Fill" Value="Black" />
                                                                                        <Setter Property="Stroke" Value="Yellow" />
                                                                                    </DataTrigger>
                                                                                    <DataTrigger Binding="{Binding BonusType}" Value="{x:Static resources:ResourceType.Plant}">
                                                                                        <Setter Property="Data" Value="{Binding Source={StaticResource leaf}, Path=Data}" />
                                                                                        <Setter Property="Fill" Value="Green" />
                                                                                    </DataTrigger>
                                                                                    <DataTrigger Binding="{Binding BonusType}" Value="{x:Static resources:ResourceType.Steel}">
                                                                                        <Setter Property="Data" Value="{Binding Source={StaticResource hammer}, Path=Data}" />
                                                                                        <Setter Property="Fill" Value="Maroon" />
                                                                                    </DataTrigger>
                                                                                </Style.Triggers>
                                                                            </Style>
                                                                        </Path.Style>
                                                                    </Path>
                                                                </DataTemplate>
                                                            </ItemsControl.ItemTemplate>
                                                        </ItemsControl>
                                                    </StackPanel>
                                                </Grid>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="placeOutline" Property="Fill" Value="LightGray"></Setter>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>

                                        </Button.Template>
                                    </Button>

                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal"></StackPanel>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</Window>